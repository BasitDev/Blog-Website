<%- include('partials/header'); -%> 

<h1>Home</h1>

<p> <%= homeStarting %> </p>

 <% publishPost.forEach(function(posts){ %>
    <h1>
        <%= posts.title %>
    </h1>
    <p>
        <%= posts.post.substring(0,100) +" ..."%>
        <a href="post/<%=posts.title%>">Read More</a>
    </p>
<% }); %>

 

<%- include('partials/footer'); -%>